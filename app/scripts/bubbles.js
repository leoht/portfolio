(function ($) {

	var bubbles = [];

	for (i = 0 ; i < 6 ; i++) {
		bubbles.push({
			x: Math.random() * window.innerWidth,
			y: Math.random() * window.innerHeight
		});
	}

	window.setTimeout(function () {

	}, 500);

})(jQuery);